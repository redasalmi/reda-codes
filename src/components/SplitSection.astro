---
interface Props {
  id: string;
  name: string;
}

const { id, name } = Astro.props;
---

<section id={id} aria-labelledby={`${id}-title`} class="border-b border-border">
  <div
    class="grid grid-cols-1 md:grid-cols-[minmax(150px,0.5fr)_minmax(0,2fr)]"
  >
    <div
      class="border-b border-border px-page py-5 md:border-r md:border-b-0 md:py-[clamp(4rem,8vw,8rem)]"
    >
      <p class="type-label text-muted">{name}</p>
    </div>

    <div class="px-page py-[clamp(4rem,8vw,8rem)]">
      <slot />
    </div>
  </div>
</section>
